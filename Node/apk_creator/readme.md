#Module menager for androidjs

# Foobar

## Installation

Install the package menager [npm] 

```bash
    sudo apt-get install npm
```

Install the fs package [npm] 

```bash
    sudo npm install fs
```

## Usage
